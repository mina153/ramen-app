<div class="EventPage">
  <div class="Account__info">
   
      <h1 >EventPage </h1>

    <div class="Account_link">
      <% if user_signed_in? %>
    
          <%= link_to '⇨ MyPage', items_path(current_user), class: "share"%>
        or
          <%= link_to '🐰 SharePage 🐰', sharings_path, class: "share"%>
        
        
          <%= link_to '⇨ Logout', destroy_user_session_path, method: :delete, class: "logout" %>
        
      <% else %>
        <ul>
        <li><%= link_to '⇩ Login', new_user_session_path, class: "login" %></li>
        <li><%= link_to '⇩ New Registration', new_user_registration_path, class: "sign-up" %></li>
        </ul>
      <% end %>
    </div>

  </div>

  <div class='item1-contents'>
    <%# <div class="item__info">
      
    </div> %>
    
   
    <div class = "list">
      <% @events.each do |event| %>
        <% if current_user.id == event.user_id %>
          <ul class= "list-list"> 
            <li class='show-list'>  

                    <div class='item-info'>
                      <h3 class='event-name'>
                        <%= link_to event.theme, event_path(event.id), method: :get %>    
                      </h3>

                      <div class='event2-time'>
                        <br>
                          <%=  event.date%>    
                            
                          ( <%= event.start_at.strftime( "%H:%M" ) %>
                            〜
                          <%= event.finish_at.strftime( "%H:%M" ) %> )
                        <br>
                      </div>
                        <%# <div class='item-img-content'>
                          <%= image_tag event.image, class: "item-img" if item.image.attached?%>
                        <%# </div> %> 

                      <%# <div class='user-date'>
                        <span>Contributor : <%= link to item.user.name %></span>  
                      <%# </div>   %>
                      
                      <%# <ul class="more_list">
                        <% if user_signed_in? %>          
                          <%# <% if current_user == event.user %>  
                            <%# <div class = "edit1-destroy1">
                              <%# <%= link_to "EDIT 📝", edit_event_path(event.id), method: :get, class: "item-edit" %> 
                              <%# <%= link_to "DELETE ❌", event_path(event.id), method: :delete, class:"item-destroy" %> 
                            <%# </div> %>
                          <%# <% end %>  
                        <%# <% end %>  
                      <%# </ul>   %> 
                    </div>   
            </li>
          </ul>
        <% end %>  
      <% end %> 
    </div>
  

      
    <div class="entry-form">
      <%= link_to ' + Share event 🏝', new_event_path %>
    </div>

  </div>
  
</div>